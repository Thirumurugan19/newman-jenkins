{
  "name": "newman-jenkins",
  "version": "1.0.0",
  "description": "Project for serverless API testing",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "serverless-api-test": "newman run tests/taxProfiles.json -e tests/env.json -r cli"
  },
  "author": "Thirumurugan",
  "dependencies": {
    "newman": "^3.9.3"
  }
}
